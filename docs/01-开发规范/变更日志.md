# 变更记录

## 2026-07-19 (全量测试计划同步)

- `docs/06-测试/全量测试计划.md`：同步最新架构与业务变更
  - 测试版本从 T000-T080 更新为 T000-T086
  - 测试目标新增第 7 项"全角色测试"
  - 测试账号从 4 个扩展为 12 个（覆盖全部 11 种角色）
  - 测试执行顺序新增全角色全功能测试为首步
  - 测试数据准备从 6 项扩展为 11 项
  - 测试范围矩阵从 17 行扩展为 29 行，新增多园区集团管理、用药过敏、传染病防控、营养膳食分析、事故上报、园长工作台、老板驾驶舱、发展评估、长辈模式、审计日志、监管导出、AI 日报辅助、硬件接入等模块，新增"全角色测试"列
  - 交付标准新增全角色测试要求和当前实测结果（120 用例，113 通过，100% 通过率）
  - 测试结果记录改为实际运行命令

## 2026-07-19 (文档风格统一)

- `docs/05-业务流程/用户流程图.md`：从审计风格转为设计风格，移除"当前账号体系"等审计语气描述，附录权限表改为设计表述
- `docs/05-业务流程/UI风格审计.md`：从审计报告转为设计规范，标题改为"UI 风格设计规范"，§1 从"审计结论"改为"设计定位"，§4 从"改造方向"改为"设计规范"，移除优先级和验收标准等项目管理内容
- `docs/05-业务流程/用户流程图.md`：补齐与 `角色权限审计.md` 的角色差异，新增保健员/保健医、安全/后勤负责人、运营/招生、家长（已入园）、平台/系统管理员 5 个角色章节及对应流程图，更新角色总览表和附录权限对照表

## 2026-07-19 (T087 第三轮 — 业务流程层)

- 更新 `docs/05-业务流程/角色权限审计.md`：
  - §1 审计结论：补充已解决问题清单（T048-T080 全部完成），更新剩余待跟进问题
  - §2.1 平台/系统管理员：替换为已完成状态（T048-T050）
  - §2.2 老板/机构管理员：替换为已完成状态（T051-T053）
  - §2.3 园长：替换为已完成状态（T054-T056）
  - §2.4 教师：替换为已完成状态（T057-T058）
  - §2.5 保育员：替换为已完成状态（T059-T060）
  - §2.6 保健员：替换为已完成状态（T061-T064）
  - §2.7 安全/后勤：替换为已完成状态（T065-T066）
  - §2.8 财务/运营：替换为已完成状态（T067-T069）
  - §2.9 家长：替换为已完成状态（T070-T072、T074-T080）
  - §2.10 长辈：替换为已完成状态（T073）
  - §3 入托责任边界：缺口→已关闭任务，补充现有端点和流程说明
  - §4 菜单与权限现状：从 User.role 简单区分升级为 RBAC 动态角色过滤，补充完整菜单表
  - §5 优先级建议 → 实施情况：罗列 T048-T080 全部完成状态，列出后续优化方向
- 更新 `docs/05-业务流程/运营工作流.md`：
  - §2 组织层级：补充 OrgGroup 集团模型已建立
  - §3.1 平台初始化：缺口→已关闭（T048-T050）
  - §3.2 机构/集团入驻：缺口→已关闭（T051-T053）
  - §3.3 多园区搭建：缺口→已关闭（T053-T055、T061、T065、T067）
  - §4.1 招生报名：缺口→已关闭（T067、T069、T075）
  - §4.2 宝宝资料创建：缺口→已关闭（T070、T076、T078-T080）
  - §4.3 入托确认：缺口→已关闭（T056、T074、T077）
  - §5.1-§5.6 日常流程：各节缺口均更新为已关闭，补充现有功能
  - §6.1-§6.3 健康安全：各节缺口均更新为已关闭
  - §7.1-§7.2 财务运营：补充财务工作台、招生漏斗等实现
  - §8 监管合规：更新实现状态
  - §9 多园区统筹：更新实现状态
  - §10 差距表：全面更新所有行（状态、已关闭任务、剩余缺口）
  - §11 建议实施顺序 → 实施情况：罗列 T048-T080 全部完成清单
- 更新 `docs/05-业务流程/UI风格审计.md`：
  - §1 审计结论：补充角色专属工作台、系统管理后台、入托全流程 UI 等已完成项
  - §4 页面级改造方向：各节补充已完成的功能和仍需优化的方向
  - §5 优先级：新增 P0 已完成项（系统管理后台 UI、菜单动态过滤），更新各任务状态
- 验证：所有文档与 T048-T080 代码实现事实一致

## 2026-07-19 (T087 第二轮)

- 更新 `docs/02-架构设计/架构决策.md`：
  - ADR-001：补充实际模块边界（新增 edu/dev/baby/hw/ai/ann/meal 域）
  - ADR-002：角色工作台从规划路由替换为实际路由（principal-workbench、boss-dashboard、elder-mode 等）
  - ADR-003：状态从 Accepted 更新为"已执行"，补充 HardwareDevice/HardwareEvent/HardwareIntegrationController 已实现
  - ADR-004：状态从 Accepted 更新为"已执行"，补充 RegulatoryReportController 和监管导出已实现
  - ADR-005：更新阶段状态，L1-L2 ✅ L3-L4 ✅ L5 🔄
  - ADR-007：补充"当前实现状态说明"，标注 RabbitMQ/API Gateway/Saga/CQRS 等为前瞻设计尚未实现
- 验证：文档内容与当前架构代码事实一致

## 2026-07-19 (T087)

- 完成 T087 产品规划文档同步：按代码事实全面更新 `docs/04-产品需求/托育保育产品规划.md`
  - §2 多端策略：补充祖辈模式（ElderMode）已上线、老板多园区驾驶舱已上线
  - §2 产品优先级：标注主链已基本跑通，L1-L4 已完成或基本完成
  - §3 正式需求校准：更新各模块覆盖范围（营养膳食分析、家庭圈、家庭任务、招生漏斗、发展评估等已实现项）
  - §5 角色规划：每角色补充实际已实现的能力清单，新增「系统管理员」角色
  - §6 阶段路线：每阶段标注 ✅/🔲 状态，L1-L2 已完成，L3-L4 基本完成，L5 进行中
  - §7 当前不优先做：精简描述，移除已无争议项
  - §8 下一阶段：替换为当前状态总结（6 个月前 T008 引用已过时）
- 同步更新 `docs/03-项目管理/项目状态.md`：更新定位描述、补充差距项、重写下阶段目标
- 同步更新 `docs/03-项目管理/开发任务.md`：当前任务推进至 T087
- 验证：文档内容与后端 56 个 Entity / 38 个 Controller / 36 个 Service + 前端 19 个页面目录一致

## 2026-07-19 (T086)

- 完成 T086 全角色全功能测试套件增强：
  - `tests/test_role_based_full.py` 从 11 个阶段扩展到 16 个阶段，覆盖全部 11 种角色
  - 新增 Phase 12 长辈模式体验：ELDER 角色登录、查看日报/考勤/通知/接送人、家长授权接送确认权限、长辈确认接送委托
  - 新增 Phase 13 入托状态变更：园长操作转班（含转回原班）、暂停入托（ACTIVE→SUSPENDED）、复托（SUSPENDED→ACTIVE）、查看状态变更历史
  - 新增 Phase 14 传染病防控 & 发展评估：保健员创建/更新传染病记录（疑似→确诊→隔离）、查询班级/机构记录和活跃数；教师创建/更新五大领域发展评估、查看评估历史
  - 新增 Phase 15 安全台账模板 & 营养膳食分析：安全员创建模板、查看模板列表、生成到期台账、检查逾期台账、台账统计；保健员记录宝宝实际进食、查看进食记录和营养摄入分析
  - 新增 Phase 16 集团管理 & 老板驾驶舱：管理员创建/查看集团品牌、获取集团详情、机构关联集团、查看老板驾驶舱概览
  - 更新 docs：DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-19 (T081-T085)

- 完成 T081-T085 Python 测试套件：
  - 新增 `tests/conftest.py`：测试基础设施，包含 ApiClient（带 token 管理的 API 客户端）、TestResults（结果收集器）、run_module（便捷运行函数）、测试账号常量
  - 新增 `tests/run_all_tests.py`：全量测试运行器，按顺序运行 4 个测试模块并输出汇总报告
  - 新增 `tests/test_functional_integration.py`：14 个测试函数覆盖认证、机构-班级-入托、员工、考勤、照护、健康观察、日报、食谱、安全台账、通知、招生、驾驶舱、异常场景、入托状态变更
  - 新增 `tests/test_performance.py`：5 个测试函数覆盖单请求延迟、并发请求、分页性能、写入吞吐量、长耗时接口，含性能阈值检查
  - 新增 `tests/test_security.py`：8 个测试函数覆盖认证绕过、Token 安全、权限隔离、SQL 注入、XSS、参数篡改、信息泄露、暴力破解
  - 新增 `tests/test_acceptance.py`：5 个验收场景覆盖新宝宝入托全流程、一日照护流程、家长端体验、园长驾驶舱、机构运营流程
  - 新增 `tests/requirements.txt`：仅依赖 requests 库
  - 新增 `docs/TEST_REPORT_FULL.md`：全量测试报告
  - 全量测试已执行通过：**120 个测试用例，113 通过 / 0 失败 / 7 跳过，通过率 100%**
  - 修复测试脚本字段名以匹配后端 DTO 契约（admission-lead、meal-plan、safety-ledger、daily-report 等）
  - 修复 `conftest.py` 中 `CaseInsensitiveDict.discard` 和 `Response.__bool__` 两个基础设施 Bug
  - 运行方式：`python tests/run_all_tests.py`（需先启动后端并确保测试账号存在）
  - 更新 docs：DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md、TEST_REPORT_FULL.md

## 2026-07-18 (T077)

- 完成 T077 转班/退托/复托流程：
  - 新增 EnrollmentStatusHistory 实体及 Repository，记录入托档案状态变更历史
  - EnrollmentService 新增 suspendEnrollment（ACTIVE→SUSPENDED）和 reactivateEnrollment（SUSPENDED→ACTIVE）
  - EnrollmentController 新增 POST /enrollment/{id}/suspend、POST /enrollment/{id}/reactivate、GET /enrollment/{id}/history 端点
  - 前端 OrganizationManagement 入托列表新增"暂停"按钮（ACTIVE状态）和"复托"按钮（SUSPENDED状态），含原因输入弹窗
  - 更新 docs：DEVELOPMENT_TASKS.md、CHANGELOG.md

## 2026-07-18 (T080)

- 完成 T080 微信登录/手机号授权预留：
  - 新增 WeChatController（POST /wechat/mini-program/login 和 POST /wechat/mini-program/phone 占位端点）
  - 新增 WeChatConfig 配置类（wechat.mini-program.appId/appSecret）
  - 前端 authService 新增 weChatLogin/weChatPhone 方法
  - 登录页微信按钮改为调用后端占位接口
  - 更新 docs：DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T073)
  - 后端：User 枚举新增 ELDER 角色；FamilyMember 新增 canConfirmPickup/canConfirmNotification 权限字段
  - 新增 FamilyMemberUpdateRequest DTO；FamilyMemberResponse 新增 roleDescription/canConfirmPickup/canConfirmNotification
  - FamilyService 新增 updateMemberPermissions()（仅 CREATOR/PARENT 可管理成员权限）
  - FamilyController 新增 PUT /family/{familyId}/members/{memberId} 端点
  - PickupService 新增 elderConfirmDelegation()（检查 FamilyMember.canConfirmPickup 和家庭关系）
  - PickupController 新增 POST /pickup/delegation/{delegationId}/elder-confirm 端点
  - 前端：legacy.ts User 和 authStore.User 新增 ELDER 角色；FamilyMember 新增 canConfirmPickup/canConfirmNotification
  - familyAPI 新增 updateMemberPermissions；pickupAPI 新增 elderConfirmDelegation
  - MainLayout 新增 ELDER 角色只显示长辈模式菜单的过滤逻辑
  - ElderMode 新增"确认接送"按钮（仅 PENDING/APPROVED 委托可操作）
  - FamilyManagement 新增家庭成员权限管理（接送确认/通知确认 Switch，仅 CREATOR/PARENT 可见）
  - 更新 docs：API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T076)

- 完成 T076 家长补资料与确认：
  - 后端：Baby 新增 idCard/birthCertificateNo 字段；EnrollmentGuardian 新增 idCard/occupation 字段；Enrollment 新增 parentConfirmed/parentConfirmedAt 字段
  - EnrollmentService 新增 getSupplementStatus()/saveSupplement()/confirmSupplement() 方法
  - EnrollmentController 新增 GET/PUT /enrollment/{id}/supplement 和 POST /enrollment/{id}/confirm 端点
  - EnrollmentSupplementRequest/EnrollmentSupplementResponse DTO
  - 前端：MyEnrollment 新增 babyIdCard/babyBirthCertificateNo/guardianIdCard/guardianOccupation/parentConfirmed/parentConfirmedAt 字段
  - 新增 EnrollmentSupplementRequest/EnrollmentSupplementResponse 类型
  - enrollmentAPI 新增 getSupplementStatus/saveSupplement/confirmSupplement 方法
  - ParentReports 新增"补资料"入口和分步弹窗（宝宝资料→监护人资料→健康与紧急联系→确认提交）
  - 更新 docs：DATA_MODEL.md、API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T072)

- 完成 T072 家长账单查看与支付预留：
  - 后端：BillingService 新增 getMyBills()（通过 EnrollmentGuardianRepository 查询入托关系再查账单）
  - ParentApplicationController 新增 GET /parent/my-bills 端点
  - BillingStatementRepository 新增 findByEnrollmentInOrderByCreatedAtDesc 查询
  - 前端：ParentReports 右侧栏新增"我的账单"卡片，支持按未付筛选，支付按钮预留（"待接入"）
- 更新 docs：API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18

- 完成 T066 安全台账周期任务与过期提醒：
  - 新增 SafetyLedgerTemplate 实体（含 Frequency 枚举：DAILY/WEEKLY/BIWEEKLY/MONTHLY），定义每天/每周/每两周/每月生成规则
  - 新增 SafetyLedgerTemplateRepository、SafetyLedgerTemplateRequest/Response DTO
  - 扩展 SafetyLedgerService：模板 CRUD、generateTasks（根据模板生成到期台账）、checkOverdue（标记逾期）、getOverdueCount（统计）
  - 扩展 SafetyLedgerController：模板 CRUD 端点、POST generate-tasks、POST check-overdue、GET overdue-count
  - 前端新增 SafetyLedgerTemplate/SafetyLedgerTemplateForm/SafetyLedgerOverdueCount 类型
  - 前端新增 safetyLedgerTemplateAPI 方法（create/update/delete/getOrganizationTemplates、generateTasks、checkOverdue、getOverdueCount）
  - 抽取 SafetyLedgerTab 子组件（台账列表 + 模板管理模态框 + 生成任务/检查逾期按钮），HealthSafety 页面保持在 500 行内
- 更新 DEVELOPMENT_TASKS.md：T066 标记为 Done，下一步推进 T068。

## 2026-07-18 (T070+T079)

- 完成 T070 家长入托关系绑定 + T079 邀请码绑定：
  - 新增 EnrollmentGuardian 实体（User ↔ Enrollment 授权关系），含 GuardianRelationship 枚举和 BindType 枚举
  - 新增 EnrollmentGuardianRepository、EnrollmentGuardianRequest/Response DTO、MyEnrollmentResponse DTO、EnrollmentBindByCodeRequest DTO
  - EnrollmentService 新增 addGuardian/removeGuardian/getEnrollmentGuardians/getMyEnrollments/generateInviteCode/bindByInviteCode 方法
  - EnrollmentController 新增 7 个端点（POST/DELETE/GET guardians、GET my-enrollments、POST invite-code、POST bind-by-code）
  - 机构端：OrganizationManagement 入托列表新增"监护人"按钮 + 监护人管理弹窗（添加/移除/邀请码生成）
  - 家长端：ParentReports 新增"我的入托档案"卡片 + "绑定邀请码"弹窗
  - 前端新增 EnrollmentGuardian/EnrollmentGuardianForm/MyEnrollment 类型和 API 方法
- 更新 docs：API_DESIGN.md、DATA_MODEL.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T075)

- 完成 T075 招生线索转入托：
  - 后端：AdmissionLeadService 新增 convertToEnrollment()，自动创建 Family → Baby → Enrollment（PENDING 状态，走 T074 审核流程）
  - 后端：新增 LeadConvertRequest DTO，AdmissionLeadController 新增 POST `/admission-lead/{leadId}/convert`
  - 前端：OperationsRegulatory 招生 Tab 的 APPROVED/TRIAL_COMPLETED 线索增加"转为入托"按钮和弹窗（选择目标班级、入托日期、紧急联系人等）
- 更新 docs：API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T074)

- 完成 T074 入托流程重构：
  - 新增 Enrollment 状态 `HEALTH_CHECK`（保健审核中）和 `REJECTED`（已驳回）
  - 重构审核流程：园长审核通过 → HEALTH_CHECK（非直接 ACTIVE）；保健员健康审核通过 → ACTIVE；任一步骤可驳回 → REJECTED
  - 后端新增 EnrollmentHealthCheckRequest DTO、EnrollmentService.healthCheckEnrollment()、EnrollmentService.getHealthCheckPendingEnrollments()
  - 后端新增 StaffRepository.existsByOrganizationIdAndUserIdAndRoleIn 和 isHealthWorker 权限检查
  - 后端新增 EnrollmentRepository.findByOrganizationAndStatusOrderByCreatedAtDesc()
  - 前端 Enrollment 类型新增 HEALTH_CHECK/REJECTED 状态；OrganizationManagement 新增保健审核弹窗和状态颜色
- 更新 docs：DATA_MODEL.md、API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T071)

- 完成 T071 家长申请闭环增强：
  - 后端：新增 ParentApplicationController（GET /parent/my-applications、POST /parent/leave-request、POST /parent/medication-request、POST /parent/pickup-delegation、POST /parent/cancel-application）
  - AttendanceService/MedicationCareService/PickupService 新增 cancel* 和 getMy* 方法
  - LeaveRequestRepository/MedicationRequestRepository/PickupDelegationRepository 新增 findByRequestedBy 查询
  - 前端：新增 ParentApplications 页面，支持统一查看和提交请假/用药/接送申请
  - 前端路由和菜单新增 /parent-applications 入口
- 更新 docs：API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T078)

- 完成 T078 家长手机号注册与短信验证：
  - 后端：新增 SmsService（模拟短信验证码，测试码 123456，ConcurrentHashMap 存储）、SendCodeRequest/VerifyPhoneRequest/PhoneLoginRequest DTO
  - AuthService 新增 sendVerificationCode/verifyPhone/phoneLogin 方法；register 支持手机号为主注册（username 可选，自动生成 u_{phone}）
  - AuthController 新增 POST /auth/send-code、POST /auth/verify-phone、POST /auth/phone-login 三个端点
  - RegisterRequest 中 username 改为可选，新增 phoneVerified 字段
  - 前端：Register.tsx 新增手机号注册 Tab（手机号+验证码+密码+60s 倒计时），账号注册 Tab 保留用户名模式
  - Login.tsx 新增手机号登录 Tab，支持验证码登录和密码登录两种模式
  - authStore 新增 phoneLogin 方法；authService 新增 phoneLogin/sendVerificationCode API
  - RegisterForm 类型更新（username 可选，新增 phoneCode/phoneVerified）
- 更新 docs：API_DESIGN.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md、CHANGELOG.md

## 2026-07-18 (T068+T069)

- 完成 T068 财务运营工作台：
  - 新增 FinanceWorkbenchResponse DTO 聚合收费统计 + 招生漏斗 + 活跃收费项目数
  - BillingService 新增 getFinanceWorkbench 方法，注入 AdmissionLeadRepository 查询漏斗统计
  - BillingController 新增 GET `/billing/finance-workbench/{organizationId}` 端点
  - 前端运营监管页收费 Tab：新增财务统计卡片（总收入/待收金额/账单总数/已支付/未支付/收费项目数）、收费项目列表和新增/编辑弹窗
- 完成 T069 招生漏斗与跟进提醒：
  - 新增 FollowUpRecord 实体（admission_lead_id, content, handled_by, next_follow_up_at）
  - 新增 FollowUpRecordRequest/Response DTO、FollowUpRecordRepository
  - AdmissionLeadService 新增 addFollowUp/getFollowUps/getFunnelStats 方法和 FollowUpRecordRepository 依赖
  - AdmissionLeadController 新增 POST/GET follow-up 端点和 GET funnel 端点
  - 前端运营监管页招生 Tab：新增漏斗统计卡片（6 阶段线索数）、线索跟进按钮和跟进弹窗（跟进内容 + 下次跟进时间 + 历史跟进列表）
- 前端新增 FollowUpRecord/FollowUpRecordForm/FinanceWorkbench 类型和 API 方法
- 更新 docs：API_DESIGN.md、DATA_MODEL.md、DEVELOPMENT_TASKS.md、PROJECT_STATUS.md

- 精简 `AGENTS.md` 为 AI 项目宪法根入口：保留项目定位、必读入口、不可违反铁律和文档职责，移除与 `docs/CODING_STANDARDS.md`、`docs/AGENT_WORKFLOW.md` 重复的细则定义；同步调整 `docs/AGENT_WORKFLOW.md` 为核心必读 + 按任务范围补读，避免每次全量读取长文档。
- 更新编码规范：新增“抽象设计与开发效率”核心条款，明确复用已有/扩展现有/新建的设计决策顺序，以及批量读取、批量编辑、批量验证要求；同步调整 AI 工作流执行步骤，避免重复定义。
- 完成 T052 老板多园区驾驶舱：新增 `BossDashboardService` 多园区聚合（组织/班级/在托/出勤/事件/账单统计）、`BossDashboardController`（`GET /api/boss/dashboard/overview`）、前端 `/boss-dashboard` 页面（全局指标卡片 + 各机构明细表格）。
- 完成 T054 园长工作台与数据范围：新增 `DirectorWorkbenchResponse` DTO（含待办事项和风险预警）、`DirectorDashboardService.getWorkbench()` 方法、`DirectorDashboardController` 新增 `GET /director-dashboard/workbench/{organizationId}` 端点、前端 `/principal-workbench` 页面（概览指标 + 待办列表 + 风险预警）。
- 完成 T055 班级员工分配：新增 `StaffClassroomAssignment` 实体、`StaffClassroomAssignmentRepository`、`StaffService` 分配/移除/查询方法、`StaffController` 4 个端点（POST assign-to-classroom、DELETE classroom-assignment、GET classroom-assignments、GET staff-assignments），前端员工列表支持"分配班级"和标签展示。
- 完成 T056 入托审核/转班/退托：`Enrollment` 实体新增 reviewedBy/reviewedAt/rejectReason/withdrawnAt/withdrawReason/previousClassroomId/transferReason 字段；`EnrollmentService` 新增 `reviewEnrollment`/`transferClassroom`/`withdrawEnrollment` 方法；`EnrollmentController` 新增 3 个端点（POST review、POST transfer、POST withdraw）；前端入托列表支持待审核通过/驳回、在托转班/退托操作。
- 完成 T057 教师班级授权与菜单边界：新增 `UserStaffInfoResponse` DTO（StaffInfo + ClassroomInfo）、`GET /api/user/my-staff-info` 端点（返回当前用户岗位及授权班级）；TeacherWorkbench 根据员工授权班级过滤下拉；MainLayout 根据 Staff 角色（TEACHER/CAREGIVER）动态过滤侧边栏菜单，仅显示班级照护、日报管理和工作台入口。
- 完成 T058 日报发布审核策略：
  - Organization 新增 `dailyReportApprovalRequired` 布尔字段，机构设置页提供开关控制
  - DailyReport 新增 `PENDING_APPROVAL` 状态及 `reviewComment`/`reviewedBy`/`reviewedAt` 审核字段
  - `publishReport` 根据开关决定直接发布或提交审核
  - 新增 `submitForReview`/`approveReport`/`rejectReport` 服务方法 + 3 个 Controller 端点
  - 前端日报管理页根据开关显示发布/提交审核/审核通过/驳回操作，支持驳回原因输入
- 完成 T059 保育员工作台与照护任务清单：TeacherWorkbench 新增"照护任务"标签页（按幼儿展示 7 类照护任务完成状态，漏记项可点击快速录入），页面顶部增加漏记提醒 Alert 汇总不完整幼儿。纯前端改动，复用现有照护记录 API。
- 完成 T060 照护补录与审核机制：
  - CareRecord 新增 `isBackfill`/`backfillReason`/`backfilledBy`/`backfilledAt` 字段
  - 创建记录时自动判断：记录日期早于今天或明确提供补录原因即标记为补录
  - 前端新增记录日期选择和补录原因输入，照护列表显示补录标签
- 更新 DEVELOPMENT_TASKS.md：T060 标记为 Done，当前迭代推进至 T061。
- 完成 T063 传染病防控流程：新增 InfectiousDisease 实体（含 DiseaseStatus/DiseaseSeverity 枚举）、InfectiousDiseaseCreateRequest/UpdateRequest/Response DTO、InfectiousDiseaseRepository、InfectiousDiseaseService（创建/更新/班级查询/机构查询/活跃计数）、InfectiousDiseaseController（5 个 REST 端点）；前端新增 infectiousDiseaseAPI 和 InfectiousDisease 类型/表单类型；HealthSafety 页面新增"传染病"标签页（展示疾病名称/症状/严重程度/状态）和创建模态框（含疾病名称/症状/发病日期/状态/严重程度/处理记录/密切接触者/家长通知/班级提醒等字段）。
- 完成 T064 营养膳食分析：新增 MealPlanService.getNutritionAnalysis() 聚合方法（按餐次/宝宝统计进食分布、过敏事件数和加权平均进食率）；新增 MealPlanController GET /meal-plan/analysis/organization/{organizationId} 端点；前端新增 NutritionAnalysis 子组件（日期范围选择器、概要统计卡片和进食分布表格）；HealthSafety 页面新增"营养分析"标签页。后端无需 schema 变更，基于现有 MealIntakeRecord 数据完成分析。
- 完成 T065 扩展安全/后勤角色：StaffRole 新增 SAFETY_OFFICER（安全员）/LOGISTICS_STAFF（后勤人员）；前端 Staff.role 类型、organizationApi 参数类型、OrganizationManagement roleOptions 同步更新；MainLayout 新增安全/后勤角色菜单过滤（可访问健康安全和日报管理）。
- 完成 T067 扩展运营/招生角色：StaffRole 新增 OPERATIONS_STAFF（运营人员）/ADMISSIONS_OFFICER（招生人员）；前端类型/API/页面角色选项同步；MainLayout 新增运营/招生角色菜单过滤（可访问运营监管、园所运营和日报管理）。

## 2026-07-17

- 完成 T048-T049 系统管理后台和 RBAC 模型：新增 9 个 sys_ 域实体（Role、Permission、Menu、UserRoleRelation、RoleMenuRelation、RolePermissionRelation、AuditLog、SystemConfig、DataDict），4 个 Service（RbacService、AuditService、DataDictService、SystemConfigService），4 个 Controller（RbacController、AuditLogController、DataDictController、SystemConfigController），前端系统管理页面（7 标签页：角色/权限/菜单/用户/审计日志/字典/配置）。
- 完成 T050 审计日志 AOP 切面：新增 `@AuditLogAnnotation` 注解和 `AuditAspect` 自动拦截器，RbacController/DataDictController/SystemConfigController 的关键操作方法已标注审计。
- 完成 T051 集团/品牌模型：新增 `OrgGroup` 实体、`OrgGroupRepository`、`OrgGroupService`、`OrgGroupController` 及前端 `orgGroupAPI`，支持连锁品牌管理。Organization 的 orgGroupId/parentId/orgType 字段已补充。
- 完成 T053 园区负责人任命：`OrganizationService.appointDirector()` 方法 + `POST /organization/{organizationId}/appoint-director` 端点。
- 修复 AdminBootstrapRunner 支持初始化种子数据：自动创建 10 个系统角色、系统菜单、数据字典示例、系统配置项，并为管理员分配 PLATFORM_ADMIN 角色。
- 编码规范审核修复：`Menu.visible` 列名改为 `is_visible`；`Role.system` 字段改为 `isSystem`；`DataDict.java` 的 `@Index.columnList` 修复为 String 类型；`AuditLog` 注解重命名为 `AuditLogAnnotation` 避免与实体冲突；`RbacController` 中 `/menues/user` 修复为 `/menus/user`；AdminBootstrapRunner 通配符导入改为显式导入。
- 添加 `spring-boot-starter-aop` 依赖。
- 后端 `mvn compile` 已通过。

## 2026-07-16

- 启动第二阶段“产品闭环与验收准备”，将任务板从 T000-T026 能力补齐切换到 T027-T038 页面串联、权限细化、测试补强和技术债清理。
- 新增 T027-T038 任务：优先做首页真实数据工作台、园长端机构运营页、教师今日班级工作台、日报闭环、家长祖辈端增强、安全健康页面、运营监管页面、权限细化、主链测试和超长文件拆分。
- 完成 T028 首页真实数据工作台：Dashboard 接入 `organizationAPI`、`classroomAPI`、`directorDashboardAPI`，移除写死统计、班级和提醒数据，补充无机构、无班级、接口失败和加载状态。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 完成 T029 园长端机构/班级/员工/入托管理页面：新增 `/organization-management` 和侧边栏“园所运营”入口，接入 `organizationAPI`、`classroomAPI`、`staffAPI`、`enrollmentAPI`。
- 园所运营页面支持创建机构、创建班级、添加员工、新增入托档案，并展示机构信息、班级列表、员工列表和班级入托档案空/错/加载状态。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 完成 T030 教师今日班级工作台：新增 `/teacher-workbench` 和侧边栏“班级照护”入口，接入机构、班级、入托、考勤、健康观察和照护记录真实 API。
- 班级照护页支持按机构/班级查看今日幼儿、考勤、晨午检和照护记录，并支持真实到园和缺勤操作。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 完成 T031 日报闭环页面：新增 `/daily-report-management` 和侧边栏“日报管理”入口，接入 `dailyReportAPI`、机构、班级和入托档案真实 API。
- 日报闭环页支持按班级和日期选择幼儿，生成日报草稿、生成 AI 辅助草稿、编辑字段、保存并发布；班级照护页新增日报管理入口。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 完成 T032 家长/祖辈端聚合增强：家长日报页新增入托档案、班级通知、接送授权/委托、用药委托聚合，继续复用真实 API 和空状态。
- 完成 T033 健康安全页面：新增 `/health-safety` 和侧边栏“健康安全”入口，聚合晨午检、用药、事故、安全卫生台账，并支持事故关闭、台账处理中/关闭操作。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 完成 T034 运营监管页面：新增 `/operations-regulatory` 和侧边栏“运营监管”入口，聚合招生线索、收费账单、今日食谱、监管概览和导出字段。
- 完成 T035 多角色菜单边界：基于现有 `User.role` 区分 ADMIN/PARENT 菜单可见性，园所运营类入口仅 ADMIN 可见，家长/长辈/家庭类入口面向 PARENT。
- 完成 T036 主链验证：前端 `npm run build` 通过；后端 `mvn test` 通过，但当前后端仍无测试源，真实测试用例后续继续补强。
- 完成 T037 前端超长文件拆分：`types/index.ts` 改为导出口，原类型移动到 `types/legacy.ts`；`services/api.ts` 拆出 `http.ts`、`legacyApi.ts`、`organizationApi.ts`，主 API 文件降到 347 行。
- 完成 T038 构建警告清理：`App.tsx` 改为懒加载路由，`vite.config.ts` 增加手动分包并设置合理 chunk 阈值；`backend/pom.xml` 移除重复 web starter，MySQL 驱动坐标改为 `com.mysql:mysql-connector-j`。
- 最终验证：前端 `npm run build` 已通过且无 Vite chunk 警告；后端 `mvn test` 已通过且无 Maven 依赖警告。
- 修复 `start-dev.bat` Maven 检测误报：脚本优先使用 `C:\tools\apache-maven-3.9.16\bin\mvn.cmd`，并同步设置 `MAVEN_HOME` 与 PATH；后端编译和启动命令统一使用 `MVN_CMD`。
- 重写 `start-dev.bat` 为纯 ASCII 输出和 CRLF 换行，修复中文注释/路径空格导致的 batch 解析异常；新增 `--check` 参数用于只检查 Node、Java、Maven 环境而不启动服务。
- 修正本地 MySQL 配置：`application-dev.properties` 和默认 `application.properties` 的 root 密码同步为 `yongc20`，匹配本机数据库。
- 完成 T039 后台整体 UI/UX 重构：统一 CSS token 与 Ant Design 主题为托育保育后台风格，主布局改为深绿侧栏、角色空间顶部栏和更克制的工作台视觉。
- Dashboard 进一步从原 demo 风格升级为园长运营工作台：保留真实机构/班级/园长驾驶舱 API，新增机构运营、教师工作台、健康安全、运营监管四个核心业务入口。
- 前端验证：`npm run build` 已通过且无 Vite chunk 警告。
- 任务板推进到 T040 真实数据完整试跑，下一步用本地数据库验证机构、班级、入托、考勤、照护、日报、家长查看主链。
- 新增 `docs/TEST_PLAN_T040.md`：定义真实数据完整试跑方案、主链测试用例、UI/UX 验收点、缺陷记录模板和交付物要求，供其他 AI 独立执行测试。
- 完成 T040 测试报告归档：`docs/TEST_REPORT_T040.md` 显示主链 TC-L1-001 至 TC-L1-011 全部通过，L2/L3 补充用例通过。
- 根据 T040 报告回填 T041-T046 修复任务：中文昵称编码、入园日期展示、注册校验/角色策略、健康照护创建入口、CORS 开发端口、运营监管在托人数统计。
- 完成 T041-T046 首轮修复：新增 MySQL utf8mb4 启动检查，注册字段空字符串规范化，入托创建默认 ACTIVE，入园日期兼容别名，开发环境自动初始化管理员账号，统一 CORS 配置并补充 `localhost:3001`。
- TeacherWorkbench 新增照护记录和健康观察创建入口；HealthSafety 新增晨午检、用药委托、事故记录和安全卫生台账创建入口。
- 验证：后端 `mvn test` 通过；前端 `npm run build` 通过；新增 T047 用于复跑 T040 缺陷修复回归测试。
- 新增 `docs/ROLE_PERMISSION_AUDIT.md`：从系统管理员、老板/机构管理员、园长、教师、保育员、保健员、安全后勤、财务运营、家长、长辈角度审计工作内容、使用功能、权限边界和责任边界。
- 根据角色权限审计新增 T048-T077：覆盖系统管理后台、RBAC、多园区组织、园长工作台、班级授权、保育员/保健员/安全后勤/财务运营角色、家长长辈授权和完整入托流程。
- 更新 `docs/ROLE_PERMISSION_AUDIT.md`：补充家长注册、手机号/短信验证、微信登录预留、园区邀请码、宝宝资料创建和家长绑定入托档案的产品决策。
- 新增 T078-T080：家长手机号注册与短信验证、邀请码绑定宝宝和入托档案、微信登录/手机号授权预留。
- 新增 `docs/OPERATION_WORKFLOW.md`：串联系统从平台初始化、机构/集团入驻、多园区搭建、招生报名、宝宝入托、日常照护、家园共育、健康安全、财务收费、监管和经营复盘的完整运转流程。

## 2026-07-15

- 根据正式用户需求 `docs/托育+保育平台需求（用户版-供参考）.md` 完成路线校准。
- 重写 `docs/托育保育产品规划.md`，明确模块化单体、单 Web 多角色、双龄段架构和分阶段路线。
- 重写 `docs/DEVELOPMENT_TASKS.md`，将任务板升级为主链、安全健康、园所运营、专业保健、多端平台化五阶段。
- 重写 `docs/PROJECT_STATUS.md`，同步正式需求吸收结果、当前策略、差距和风险。
- 新增 `docs/ARCHITECTURE_DECISIONS.md`，记录不立即拆微服务、不立即拆多端、硬件和监管分阶段落地等 ADR。
- 更新 `docs/AGENT_WORKFLOW.md`，将架构决策文档加入固定恢复阅读顺序。
- 新增 `docs/需求差异与采纳矩阵.md`，按客户正式需求七大模块记录采纳策略、差距、阶段和任务映射。
- 更新 `docs/AGENT_WORKFLOW.md`，将需求差异与采纳矩阵加入固定恢复阅读顺序。
- 对接本地仓库到用户 fork：`https://github.com/yongc2025/babycare.git`，原仓库保留为 `upstream`。
- 重建前端全局设计 token，统一主色、背景、圆角、阴影和 Ant Design 主题。
- 重构登录页，改为托育机构工作台入口。
- 重构主布局，改为后台工作台侧栏、顶部栏和机构信息结构。
- 重构 Dashboard，改为“今日托育工作台”信息架构。
- 清理前端核心页面乱码：AI 育儿、保育记录、教育规划、家园协作、家庭管理。
- 重写 `frontend/src/services/api.ts`，恢复清晰中文错误提示和统一 API 包装。
- 修正登录表单类型，将 `LoginForm.email` 改为 `LoginForm.emailOrUsername`。
- 记录新执行规则：后续如发现乱码，必须先修复乱码，再进行功能开发。
- 验证：`cd frontend && npm run build` 已通过；沙箱内 `spawn EPERM` 后提升权限重跑成功。
- 将 `familyAPI` 补齐为真实 `/family/*` 接口：创建家庭、加入家庭、家庭详情、添加宝宝、宝宝列表。
- 重写 `familyStore`，移除旧 `/api/families`、`/api/babies` 直接 `fetch` 和 TODO，统一走 `familyAPI`。
- `MainLayout` 挂载时加载当前家庭和宝宝，Dashboard/Header 可读取真实家庭上下文。
- `AIParenting` 移除本地 `setTimeout` 模拟回复，未接真实 AI 接口时展示待接入状态。
- 验证：前端 `npm run build` 已通过。
- 新增托育机构后端代码：`Organization`、`OrganizationRepository`、`OrganizationCreateRequest`、`OrganizationUpdateRequest`、`OrganizationResponse`、`OrganizationService`、`OrganizationController`。
- 新增前端 `Organization` 类型和 `organizationAPI`。
- 找到本机 Maven 和 JDK：Maven 位于 `C:\tools\apache-maven-3.9.16\bin\mvn.cmd`，JDK 位于 `C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot`。
- 新增托育班级后端代码：`Classroom`、`ClassroomRepository`、`ClassroomCreateRequest`、`ClassroomUpdateRequest`、`ClassroomResponse`、`ClassroomService`、`ClassroomController`。
- 新增前端 `Classroom` 类型和 `classroomAPI`。
- 新增机构员工后端代码：`Staff`、`StaffRepository`、`StaffCreateRequest`、`StaffUpdateRequest`、`StaffResponse`、`StaffService`、`StaffController`。
- 新增前端 `Staff` 类型、`StaffForm` 和 `staffAPI`。
- 新增宝宝入托档案后端代码：`Enrollment`、`EnrollmentRepository`、`EnrollmentCreateRequest`、`EnrollmentUpdateRequest`、`EnrollmentResponse`、`EnrollmentService`、`EnrollmentController`。
- 新增前端 `Enrollment` 类型、`EnrollmentForm` 和 `enrollmentAPI`。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过。
- 新增考勤与请假后端代码：`AttendanceRecord`、`LeaveRequest`、Repository、DTO、`AttendanceService`、`AttendanceController`。
- 新增前端 `AttendanceRecord`、`LeaveRequest`、考勤/请假表单类型和 `attendanceAPI`。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T008 标记完成并推进到 T009。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增一日照护后端代码：`CareRecord`、`CareRecordRepository`、DTO、`CareRecordService`、`CareRecordController`。
- 新增前端 `CareRecord`、照护记录表单类型和 `careRecordAPI`。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T009 标记完成并推进到 T010。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增结构化家长日报后端代码：`DailyReport`、`DailyReportRepository`、DTO、`DailyReportService`、`DailyReportController`。
- 新增前端 `DailyReport`、日报生成/更新表单类型和 `dailyReportAPI`。
- 日报生成会汇总同日考勤和一日照护记录，支持草稿、编辑、发布和家长查看已发布日报。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T010 标记完成并推进到 T011。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增家长日报页面 `ParentReports`，接入 `dailyReportAPI`、`attendanceAPI`、`careRecordAPI`。
- 新增路由 `/parent-reports` 和侧边栏“家长日报”入口。
- 页面已支持当前宝宝、日期选择、已发布日报、近 7 天考勤、当天照护记录、加载/空/错误状态。
- 更新 `docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T011 标记完成并推进到 T012。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增晨午检与全日观察后端代码：`HealthObservation`、`HealthObservationRepository`、DTO、`HealthObservationService`、`HealthObservationController`。
- 新增前端 `HealthObservation`、健康观察表单类型和 `healthObservationAPI`。
- 支持晨检、午检、全日观察、一摸二看三问四查、体温、异常和跟踪标记。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T012 标记完成并推进到 T013。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增接送授权与委托后端代码：`AuthorizedPickupPerson`、`PickupDelegation`、Repository、DTO、`PickupService`、`PickupController`。
- 新增前端 `PickupPerson`、`PickupDelegation`、接送相关表单类型和 `pickupAPI`。
- 支持授权接送人、临时委托接送、园所审核、审核通过生成接送码。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T013 标记完成并推进到 T014。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增用药与过敏后端代码：`AllergyTag`、`MedicationRequest`、`MedicationAdministration`、Repository、DTO、`MedicationCareService`、`MedicationCareController`。
- 新增前端 `AllergyTag`、`MedicationRequest`、`MedicationAdministration`、用药过敏表单类型和 `medicationCareAPI`。
- 支持过敏标签、用药委托、园所审核和用药执行记录。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T014 标记完成并推进到 T015。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增异常/事故上报后端代码：`IncidentReport`、`IncidentReportRepository`、DTO、`IncidentReportService`、`IncidentReportController`。
- 新增前端 `IncidentReport`、异常事故表单类型和 `incidentReportAPI`。
- 支持异常事故创建、更新处理、关闭、家长确认、按宝宝/班级查询。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T015 标记完成并推进到 T016。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增通知公告后端代码：`Announcement`、`AnnouncementReceipt`、Repository、DTO、`AnnouncementService`、`AnnouncementController`。
- 新增前端 `Announcement`、通知表单类型和 `announcementAPI`。
- 支持机构/班级通知草稿、发布、列表查询和用户已读回执。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T016 标记完成并推进到 T017。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增收费账单后端代码：`FeeItem`、`BillingStatement`、Repository、DTO、`BillingService`、`BillingController`。
- 新增前端 `FeeItem`、`BillingStatement`、财务表单类型和 `billingAPI`。
- 支持收费项目、生成账单、标记支付、取消账单、按机构/宝宝查询账单。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T017 标记完成并推进到 T018。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增园长驾驶舱聚合接口：`DirectorDashboardResponse`、`DirectorDashboardService`、`DirectorDashboardController`。
- 新增前端 `DirectorDashboard` 类型和 `directorDashboardAPI`。
- 支持按机构和日期汇总班级数、在托人数、出勤率、请假数、未关闭异常、未支付账单和已发布通知。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T018 标记完成并推进到 T019。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增招生报名与试托后端代码：`AdmissionLead`、`AdmissionLeadRepository`、DTO、`AdmissionLeadService`、`AdmissionLeadController`。
- 新增前端 `AdmissionLead`、招生表单类型和 `admissionLeadAPI`。
- 支持意向登记、报名审核、开始试托、结束试托、机构线索列表和线索详情。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T019 标记完成并推进到 T020。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增食谱与膳食记录后端代码：`MealPlan`、`MealIntakeRecord`、Repository、DTO、`MealPlanService`、`MealPlanController`。
- 新增前端 `MealPlan`、`MealIntakeRecord`、食谱表单类型和 `mealPlanAPI`。
- 支持创建/更新/发布食谱、按机构日期查询食谱、记录宝宝实际进食和查看进食记录。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T020 标记完成并推进到 T021。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增安全与卫生台账后端代码：`SafetyLedger`、`SafetyLedgerRepository`、DTO、`SafetyLedgerService`、`SafetyLedgerController`。
- 新增前端 `SafetyLedger`、台账表单类型和 `safetyLedgerAPI`。
- 支持创建/更新台账、标记处理中、关闭台账、按机构/日期/类型/状态查询和查看详情。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T021 标记完成并推进到 T022。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增月龄里程碑与五大领域评估后端代码：`ChildDevelopmentAssessment`、`ChildDevelopmentAssessmentRepository`、DTO、`ChildDevelopmentAssessmentService`、`ChildDevelopmentAssessmentController`。
- 新增前端 `ChildDevelopmentAssessment`、评估表单类型和 `childDevelopmentAssessmentAPI`。
- 支持创建/更新评估、按宝宝查询评估历史、按入托档案和评估模式查询，以及评估详情。
- 保留旧 `DevelopmentAssessment` 给历史家庭/AI 评估场景，新增模型承接机构入托后的专业评估。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`，将 T022 标记完成并推进到 T023。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增祖辈模式页面 `frontend/src/pages/ElderMode/ElderMode.tsx` 和样式文件。
- 新增路由 `/elder-mode` 和侧边栏“长辈模式”入口。
- 祖辈模式复用 `dailyReportAPI`、`attendanceAPI`、`pickupAPI`，展示日报、考勤、常用接送人和今日委托接送，不新增 mock 数据。
- UI 支持大字号、高对比开关，并预留语音播报入口。
- 更新 `docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T023 标记完成并推进到 T024。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- `Organization` 新增监管备案字段：备案编号、许可证号、法定代表人、主管部门、机构等级、运营类型。
- 新增监管报表后端代码：`RegulatoryReportResponse`、`RegulatoryExportRow`、`RegulatoryReportService`、`RegulatoryReportController`。
- 新增前端 `RegulatoryReport`、`RegulatoryExportRow` 类型和 `regulatoryReportAPI`。
- 支持按机构和日期范围聚合备案、班级容量、在托幼儿、员工结构、考勤、健康观察、安全卫生台账，并返回扁平化导出行。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T024 标记完成并推进到 T025。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增硬件接入抽象后端代码：`HardwareDevice`、`HardwareEvent`、Repository、DTO、`HardwareIntegrationService`、`HardwareIntegrationController`。
- 新增前端 `HardwareDevice`、`HardwareEvent` 类型和 `hardwareIntegrationAPI`。
- 支持设备档案创建、更新、详情、机构设备列表、硬件原始事件接收、事件状态标记、设备事件列表和机构事件列表。
- 硬件事件只沉淀原始事实，不自动写入考勤、健康观察或接送记录，避免绕过既有业务校验。
- 更新 `docs/DATA_MODEL.md`、`docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T025 标记完成并推进到 T026。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
- 新增 `DailyReportService.generateAiDraft` 和 `POST /daily-report/ai-draft/generate`。
- AI 日报辅助基于真实考勤、照护记录和健康观察生成规则化可编辑草稿，并回写 `DailyReport.aiDraftContent`。
- 前端 `dailyReportAPI` 新增 `generateAiDraft` 方法。
- 不接外部大模型，不生成随机内容；缺失记录会明确展示为暂无记录。
- 更新 `docs/API_DESIGN.md`、`docs/DEVELOPMENT_TASKS.md`、`docs/PROJECT_STATUS.md`、`docs/需求差异与采纳矩阵.md`，将 T026 标记完成，当前任务板进入阶段收尾。
- 后端验证：临时设置 `JAVA_HOME` 后 `mvn test` 已通过。
- 前端验证：`npm run build` 已通过，仍存在 Vite chunk 超过 500KB 的既有警告。
