# 项目状态总览

最近更新：2026-07-19

## 当前定位

BabyCare/HuiGrowth 已从“家庭育儿记录 + 家庭协作”雏形，完成向“托育 + 保育机构运营平台”的主体迁移。

当前阶段：L1-L3 主链闭环+园所运营已基本完成，L4 专业保健与教学评估接近完成，L5 多端与硬件已启动部分能力。全量测试 120 用例 113 通过。

```text
模块化单体后端
单 Web 多角色前端
托育主链已跑通
L4-L5 能力推进中
```

## 当前技术栈

- 前端：React 18、TypeScript、Vite、Ant Design、Zustand、Axios
- 后端：Spring Boot 3.2、Java 17、Spring Security、JWT、Spring Data JPA、MySQL
- 项目宪法：`AGENTS.md`
- AI 行为指令：`.github/copilot-instructions.md`
- 工作流细则：`docs/01-开发规范/AI工作流.md`
- 编码规范：`docs/01-开发规范/编码规范.md`
- 架构决策：`docs/02-架构设计/架构决策.md`
- 需求矩阵：`docs/04-产品需求/需求差异与采纳矩阵.md`
- 任务清单：`docs/03-项目管理/开发任务.md`
- T040 测试方案：`docs/06-测试/T040测试计划.md`
- T040 测试报告：`docs/06-测试/T040测试报告.md`
- 角色权限审计：`docs/05-业务流程/角色权限审计.md`
- 系统运转流程：`docs/05-业务流程/运营工作流.md`
- 产品蓝图：`docs/04-产品需求/托育保育产品规划.md`
- 用户需求参考：`docs/04-产品需求/托育+保育平台需求（用户版-供参考）.md`

## 已完成

| 模块 | 状态 | 说明 |
|---|---|---|
| 用户认证 | 可用雏形 | 登录、注册、JWT、用户信息存储 |
| 前端基础构建 | 已恢复 | `npm run build` 已通过 |
| 全局主题 | 已重构 | Ant Design token 和 CSS 变量已改为托育系统风格 |
| 登录页 | 已重构 | 从模板登录页改为托育机构工作台入口 |
| 后台整体 UI/UX | 已重构 | T039 已完成全局 token、Ant Design 主题、主布局、左侧菜单、顶部栏和首页工作台统一 |
| 主布局 | 已重构 | 改为托育机构后台工作台侧栏、顶部栏和角色空间结构 |
| Dashboard | 已接真实数据并重构 | 接入机构、班级和园长驾驶舱 API，新增运营入口，移除首页静态 demo 统计 |
| 业务页面乱码 | 已清理一轮 | AI 育儿、保育记录、教育规划、家园协作、家庭管理已恢复清晰中文骨架 |
| 家庭状态层 | 已统一 | `familyStore` 已改为使用真实 `/family/*` API |
| 机构模型与接口 | 已实现并验证 | Organization 实体、Repository、DTO、Service、Controller 和前端 `organizationAPI` |
| 班级模型与接口 | 已实现并验证 | Classroom 实体、Repository、DTO、Service、Controller 和前端 `classroomAPI` |
| 员工模型与接口 | 已实现并验证 | Staff 实体、Repository、DTO、Service、Controller 和前端 `staffAPI` |
| 入托档案模型与接口 | 已实现并验证 | Enrollment 实体、Repository、DTO、Service、Controller 和前端 `enrollmentAPI` |
| 考勤与请假模型与接口 | 已实现并验证 | AttendanceRecord、LeaveRequest 实体、Repository、DTO、Service、Controller 和前端 `attendanceAPI` |
| 一日照护模型与接口 | 已实现并验证 | CareRecord 实体、Repository、DTO、Service、Controller 和前端 `careRecordAPI` |
| 结构化家长日报模型与接口 | 已实现并验证 | DailyReport 实体、Repository、DTO、Service、Controller 和前端 `dailyReportAPI` |
| 家长日报查看入口 | 已实现并验证 | 新增 `ParentReports` 页面，接入日报、考勤和照护记录真实 API |
| 晨午检与全日观察模型与接口 | 已实现并验证 | HealthObservation 实体、Repository、DTO、Service、Controller 和前端 `healthObservationAPI` |
| 接送授权与委托模型与接口 | 已实现并验证 | AuthorizedPickupPerson、PickupDelegation、PickupService、PickupController 和前端 `pickupAPI` |
| 用药与过敏模型与接口 | 已实现并验证 | AllergyTag、MedicationRequest、MedicationAdministration、MedicationCareService 和前端 `medicationCareAPI` |
| 异常/事故上报模型与接口 | 已实现并验证 | IncidentReport 实体、Repository、DTO、Service、Controller 和前端 `incidentReportAPI` |
| 通知公告与已读回执 | 已实现并验证 | Announcement、AnnouncementReceipt、AnnouncementService、AnnouncementController 和前端 `announcementAPI` |
| 收费项目与账单 | 已实现并验证 | FeeItem、BillingStatement、BillingService、BillingController 和前端 `billingAPI` |
| 园长驾驶舱聚合接口 | 已实现并验证 | DirectorDashboardResponse、DirectorDashboardService、DirectorDashboardController 和前端 `directorDashboardAPI` |
| 园所运营页面 | 已实现并验证 | 新增 `/organization-management`，接入机构、班级、员工和入托档案真实 API |
| 教师班级照护页 | 已实现并验证 | 新增 `/teacher-workbench`，接入机构、班级、入托、考勤、健康观察和照护记录真实 API |
| 日报闭环页面 | 已实现并验证 | 新增 `/daily-report-management`，支持日报草稿生成、AI 草稿、编辑、保存和发布 |
| 家长聚合页 | 已增强并验证 | 家长日报页聚合日报、考勤、照护、通知、接送和用药真实 API |
| 健康安全页 | 已实现并验证 | 新增 `/health-safety`，聚合晨午检、用药、事故和安全卫生台账 |
| 运营监管页 | 已实现并验证 | 新增 `/operations-regulatory`，聚合招生、收费、食谱和监管导出 |
| 前端路由分包 | 已完成 | `App.tsx` 改为懒加载路由，`vite.config.ts` 增加手动分包 |
| 前端类型/API 拆分 | 已完成 | `types/index.ts`、`services/api.ts` 已拆薄，避免继续超过硬上限 |
| Maven 警告清理 | 已完成 | 移除重复 `spring-boot-starter-web`，MySQL 驱动改为 `com.mysql:mysql-connector-j` |
| 招生报名与试托雏形 | 已实现并验证 | AdmissionLead、AdmissionLeadService、AdmissionLeadController 和前端 `admissionLeadAPI` |
| 食谱与膳食记录雏形 | 已实现并验证 | MealPlan、MealIntakeRecord、MealPlanService、MealPlanController 和前端 `mealPlanAPI` |
| 安全与卫生台账 | 已实现并验证 | SafetyLedger、SafetyLedgerService、SafetyLedgerController 和前端 `safetyLedgerAPI` |
| 月龄里程碑与五大领域评估 | 已实现并验证 | ChildDevelopmentAssessment、ChildDevelopmentAssessmentService、Controller 和前端 `childDevelopmentAssessmentAPI` |
| 祖辈模式 | 已实现并验证 | 新增 `/elder-mode` 长辈入口，复用日报、考勤、接送真实 API，支持大字、高对比和语音播报预留 |
| 监管字段与导出 | 已实现并验证 | 机构新增备案字段，新增 `RegulatoryReportController` 和前端 `regulatoryReportAPI`，提供监管聚合与导出行 |
| 硬件接入抽象 | 已实现并验证 | 新增 `HardwareDevice`、`HardwareEvent`、`HardwareIntegrationController` 和前端 `hardwareIntegrationAPI`，仅沉淀设备档案与原始事件 |
| AI 日报辅助 | 已实现并验证 | 新增 `/daily-report/ai-draft/generate`，基于真实考勤、照护和健康观察生成可编辑日报草稿 |
| 真实数据完整试跑 | 已完成 | T040 主链 TC-L1-001 至 TC-L1-011 全部通过，L2/L3 补充用例通过 |
| T040 缺陷修复 | 已完成首轮 | T041-T046 已完成代码修复，等待回归测试确认 |
| 角色权限审计 | 已完成 | 已按系统管理员、老板、园长、教师、保育员、保健员、安全后勤、财务运营、家长、长辈完成边界审计 |
| 系统运转流程 | 已完成 | 已从平台开通、机构入驻、多园区、招生、入托、照护、家园、健康安全、财务、监管串联完整流程 |
| 系统管理后台 | 已完成 | 新增系统管理入口（/system-management），包含角色管理、权限管理、菜单管理、用户管理、审计日志、数据字典、系统配置 7 个标签页 |
| RBAC 角色权限模型 | 已完成 | 9 个 sys_ 域实体：Role、Permission、Menu、UserRoleRelation、RoleMenuRelation、RolePermissionRelation、AuditLog、SystemConfig、DataDict，对应完整 Service 和 Controller |
| 审计日志 | 已完成 | @AuditLogAnnotation 注解 + AuditAspect AOP 切面，自动记录后台关键操作（角色/权限/菜单/配置变更） |
| 集团/品牌管理 | 已完成 | OrgGroup 实体、CRUD 接口和管理页面，支持连锁品牌多园区架构 |
| 老板多园区驾驶舱 | 已完成 | BossDashboardService 多园区聚合、BossDashboardController + 前端 /boss-dashboard 页面，支持多园区指标对比 |
| 园区负责人任命 | 已完成 | OrganizationService.appointDirector() + POST /organization/{id}/appoint-director 端点 |
| 园长工作台 | 已完成 | DirectorWorkbenchResponse 待办+风险预警、GET /director-dashboard/workbench/{orgId} 端点、前端 /principal-workbench 页面 |
| 教师班级授权与菜单边界 | 已完成 | UserStaffInfo 接口 + TeacherWorkbench 班级过滤 + MainLayout 菜单按角色动态过滤 |
| 日报发布审核策略 | 已完成 | Organization 开关 dailyReportApprovalRequired、DailyReport PENDING_APPROVAL 状态、submit/approve/reject 端点、前端审核 UI |
| 保育员工作台与照护任务清单 | 已完成 | TeacherWorkbench 新增照护任务标签页和漏记提醒，纯前端改动 |
| 照护补录与审核机制 | 已完成 | CareRecord 补录字段 + 自动判断 + 前端日期选择和补录原因 |
| 扩展保健员角色 | 已完成 | StaffRole 新增 HEALTH_WORKER/HEALTH_DOCTOR；前端类型/API/页面角色选项同步；MainLayout 菜单过滤支持保健员/保健医 |
| 保健工作台 | 已完成 | HealthSafety 页面增强为保健工作台，新增概览统计卡片、过敏标签和食谱标签页，支持过敏标签创建 |
| 传染病防控流程 | 已完成 | 新增 InfectiousDisease 全栈 CRUD + 前端传染病标签页和创建模态框 |
| 安全台账周期任务与过期提醒 | 已完成 | SafetyLedgerTemplate 实体 + CRUD + 周期任务生成 + 逾期检测 + 前端模板管理 |
| 家长手机号注册与短信验证 | 已完成 | 新增 SmsService（模拟短信验证码）、send-code/verify-phone/phone-login 三个端点；注册页支持手机号+验证码注册；登录页支持手机号+验证码/密码登录 |
| 家长申请闭环增强 | 已完成 | 新增 ParentApplicationController（5 个端点：我的申请列表、提交请假/用药/接送、取消申请）；新增 ParentApplications 页面，支持统一查看和提交三种申请 |
| 长辈授权与只读权限 | 已完成 | T073：User 新增 ELDER 角色；FamilyMember 新增 canConfirmPickup/canConfirmNotification；家长可在家庭管理授予长辈接送/通知确认权限；长辈模式增加确认接送按钮；MainLayout ELDER 角色只显示长辈模式菜单 |
| 转班/退托/复托流程 | 已完成 | T077：新增入托状态变更历史实体；暂停（ACTIVE→SUSPENDED）和复托（SUSPENDED→ACTIVE）端点；前端暂停/复托按钮和弹窗 |
| 微信登录/手机号授权预留 | 已完成 | T080：新增 WeChatController 占位端点（mini-program/login、mini-program/phone）；WeChatConfig 配置类；前端 weChatLogin/weChatPhone 方法；登录页微信按钮接入后端 |
| Python 测试套件 | 已完成 | T081-T085：conftest 测试基础设施（ApiClient/TestResults/run_module）+ run_all_tests 全量运行器 + 4 个测试模块（功能联调 14 用例、性能 5 用例、安全 8 用例、验收 5 场景），共 32 个测试函数 |
| 营养膳食分析 | 已完成 | 新增 NutritionAnalysis 前端页面，接入食谱营养数据分析 |
| 入托流程重构 | 已完成 | T074：入托流程全面重构，支持入托审核/转班/退托/复托全流程 |
| 招生线索转入托 | 已完成 | T075：招生线索（AdmissionLead）与入托档案（Enrollment）打通 |
| 家长补资料与确认 | 已完成 | T076：家长端补充宝宝资料、入托确认流程 |
| 家长账单查看与支付预留 | 已完成 | T072：家长端账单查看、支付接口预留 |
| 正式需求评估 | 已完成 | 已吸收用户正式需求中的双龄段、多角色、合规、安全健康和多端策略 |
| 需求采纳矩阵 | 已建立 | 按客户七大模块映射采纳策略、差距、阶段和任务 |
| 架构决策记录 | 已建立 | 记录当前不拆微服务、不拆多端、硬件和监管分阶段落地 |

## 主要差距

- **L4 待完成**：食品留样、消毒台账（实体/流程尚未实现）
- **L5 待推进**：教师移动端/PWA、家长小程序、IoT 网关、AI 日报草稿
- 缺少接送记录不可删除、滞留预警等安全接送增强能力
- 缺少容量预警、招生漏斗统计等园所运营增强能力
- 缺少硬件事件到业务记录的处理流程
- `ADMIN` 角色已拆解为 RBAC 多角色体系（系统管理员/老板/园长/教师/保育员等 9 种角色），但菜单和 API 级权限校验尚未完全接入

## 当前风险

- 历史文件曾出现编码显示异常；后续每次开发前必须用 `node`/`rg` 验证真实 UTF-8 内容。
- `application.properties` 和 `application-dev.properties` 中存在本地明文配置，后续应迁移到环境变量。
- 前端已完成路由懒加载和分包，最近 `npm run build` 无 Vite chunk 警告。
- 后端 Maven 可通过 `C:\tools\apache-maven-3.9.16\bin\mvn.cmd` 执行；`start-dev.bat` 已内置该 Maven 路径和 JDK 17 路径。
- `backend/pom.xml` 已清理重复 `spring-boot-starter-web` 声明，MySQL 驱动坐标已迁移到 `com.mysql:mysql-connector-j`。
- 正式需求中微服务、多端、硬件和监管能力范围很大，必须按 ADR 分阶段执行，避免过早复杂化。
- T040 测试发现的 6 个缺陷已完成首轮修复；中文昵称问题仍需要在真实 MySQL 环境复测确认。

## 当前架构策略

当前采用模块化单体：

```text
organization
enrollment
attendance
care
health
safety
family
finance
report
```

当前采用单 Web 多角色：

```text
director
teacher
parent
health
finance
security
```

## 下一阶段目标

当前阶段：**产品闭环收尾与质量加固**

### 收尾项（L4）

1. 食品留样流程实现
2. 消毒台账流程实现

### 推进项（L5）

3. AI 日报草稿生成
4. 教师移动端 / PWA
5. 家长小程序
6. IoT 网关与硬件事件→业务记录处理

### 质量加固

7. 全量测试覆盖增强（当前 120 用例 113 通过）
8. 权限校验全面接入
9. 空状态/错误状态/边界情况完善

## 当前迭代

当前任务：`T081-T085 Python 测试套件`

当前阶段：

```text
产品闭环与验收准备阶段
```

阶段目标：

1. 将已有后端 API 串到真实页面，减少 demo 感。T028-T034 已完成主要页面串联。
2. 形成园长端、教师/保育员端、家长/祖辈端的端到端使用路径。T029-T034 已完成基础路径。
3. 补齐空状态、错误状态、加载状态和权限边界。T035 已完成现有角色下的菜单边界。
4. 在页面闭环稳定后再做测试补强、文件拆分和构建警告清理。T036-T038 已完成构建验证和主要技术债清理。
5. 根据用户验证反馈统一后台视觉底座。T039 已完成。

下一轮建议：

- T081-T085 Python 测试套件已完成：
  - conftest.py：ApiClient 测试客户端、TestResults 结果收集器、run_module 运行器
  - run_all_tests.py：全量测试运行器，按顺序运行 4 个模块
  - test_functional_integration.py：14 个测试函数覆盖认证、机构-班级-入托、员工、考勤、照护、健康观察、日报、食谱、安全台账、通知、招生、驾驶舱、异常场景、入托状态变更
  - test_performance.py：5 个测试函数覆盖单请求延迟、并发请求、分页性能、写入吞吐量、长耗时接口
- T086 全角色全功能测试套件增强已完成：
  - test_role_based_full.py 从 11 个阶段扩展到 16 个阶段
  - 新增 Phase 12：长辈模式体验（ELDER 角色完整流程）
  - 新增 Phase 13：入托状态变更（转班/暂停/复托完整流程）
  - 新增 Phase 14：传染病防控 & 发展评估
  - 新增 Phase 15：安全台账模板管理 & 营养膳食分析
  - 新增 Phase 16：集团管理 & 老板多园区驾驶舱
  - 至此覆盖全部 11 种角色（ADMIN/PARENT/DIRECTOR/TEACHER/CAREGIVER/HEALTH_WORKER/FINANCE/SAFETY_OFFICER/OPERATIONS_STAFF/ADMISSIONS_OFFICER/ELDER）
  - test_security.py：8 个测试函数覆盖认证绕过、Token 安全、权限隔离、SQL 注入、XSS、参数篡改、信息泄露、暴力破解
  - test_acceptance.py：5 个验收场景覆盖新宝宝入托全流程、一日照护流程、家长端体验、园长驾驶舱、机构运营流程
- **全量测试结果：120 个测试用例，113 通过 / 0 失败 / 7 跳过，通过率 100%。** 详见 `docs/06-测试/全量测试报告.md`
- 运行方式：`python tests/run_all_tests.py`（需先启动后端并确保测试账号存在）
- T070 家长入托关系绑定已完成：
  - 新增 EnrollmentGuardian 实体（User ↔ Enrollment 多对一授权关系），支持机构直接绑定和邀请码绑定
  - 后端 7 个端点：监护人 CRUD + 我的入托 + 邀请码生成 + 邀请码绑定
  - 机构端：入托档案列表新增"监护人"按钮，弹出管理弹窗（添加/移除/邀请码生成）
  - 家长端：ParentReports 新增"我的入托档案"卡片和"绑定邀请码"入口
- T079 邀请码绑定流程已完成（随 T070 一并实现）。
- T078 家长手机号注册与短信验证已完成：
  - 后端：SmsService（模拟短信验证码，测试码 123456）、send-code/verify-phone/phone-login 三个端点
  - 前端：Register.tsx 新增手机号注册 Tab（手机号+验证码+密码）；Login.tsx 新增手机号登录 Tab（验证码登录/密码登录）
- T071 家长申请闭环增强已完成：
  - 后端：ParentApplicationController（5 个端点）、各 Service 新增 cancel 和 getMy* 方法
  - 前端：新增 ParentApplications 页面，支持统一查看和提交请假/用药/接送申请
- T072 家长账单查看与支付预留已完成：
  - 后端：BillingService.getMyBills() 通过入托监护人关系查询账单；ParentApplicationController 新增 GET /parent/my-bills
  - 前端：ParentReports 右侧栏新增"我的账单"卡片，支持按未付筛选，支付按钮预留（"待接入"）
- T074 入托流程重构已完成：
  - 新增入托状态 `HEALTH_CHECK`（保健审核中）和 `REJECTED`（已驳回）；审核流程变为：PENDING [园长] → HEALTH_CHECK [保健员] → ACTIVE
  - 后端：EnrollmentService 新增 `healthCheckEnrollment()` / `getHealthCheckPendingEnrollments()`，StaffRepository 新增 staff 角色查询
  - 后端：EnrollmentController 新增 POST `/enrollment/{id}/health-check` 和 GET `/enrollment/health-check-pending/{orgId}`
  - 前端：OrganizationManagement 入托列表支持显示 HEALTH_CHECK/REJECTED 状态和颜色，新增保健审核弹窗
  - T075/T076/T077 依赖此任务的新状态机
- T076 家长补资料与确认已完成：
  - 后端：Baby 新增 idCard/birthCertificateNo 字段；EnrollmentGuardian 新增 idCard/occupation 字段；Enrollment 新增 parentConfirmed/parentConfirmedAt 字段
  - 后端：EnrollmentService 新增 getSupplementStatus()/saveSupplement()/confirmSupplement() 方法
  - 后端：EnrollmentController 新增 GET/PUT /enrollment/{id}/supplement 和 POST /enrollment/{id}/confirm 端点
  - 前端：MyEnrollment/EnrollmentSupplementRequest/EnrollmentSupplementResponse 类型；enrollmentAPI 新增 3 个方法
  - 前端：ParentReports 的"我的入托档案"卡片增加"补资料"入口和分步弹窗（宝宝资料→监护人资料→健康与紧急联系→确认提交）
  - 已确认的资料不可再编辑，PENDING/HEALTH_CHECK 状态下可补充
- T075 招生线索转入托已完成：
  - 后端：AdmissionLeadService 新增 convertToEnrollment()，自动创建 Family → Baby → Enrollment（PENDING 状态走 T074 审核流程）
  - 后端：AdmissionLeadController 新增 POST `/admission-lead/{leadId}/convert`
  - 前端：OperationsRegulatory 招生 Tab 的 APPROVED/TRIAL_COMPLETED 线索显示"转为入托"按钮，弹窗选择目标班级后提交
- T047 仍需由测试侧复跑，用于确认 T040 缺陷修复是否关闭。
- 下一批建议：T073 长辈授权与只读权限、T076 家长补资料与确认

验证方式：

```text
cd backend
mvn test

cd frontend
npm run build
```

最近验证结果：T041-T046 首轮修复后，后端 `mvn test` 通过；前端 `npm run build` 通过。

## 状态维护规则

每完成一个开发任务，必须更新（交付文档规则以 `docs/01-开发规范/AI工作流.md` §7 为唯一真源）：

- `docs/03-项目管理/项目状态.md`
- `docs/03-项目管理/开发任务.md`
- 如涉及数据模型，更新 `docs/02-架构设计/数据模型.md`
- 如涉及 API，更新 `docs/02-架构设计/API设计.md`
- 如涉及架构策略，更新 `docs/02-架构设计/架构决策.md`
- 如涉及规范变更，更新 `docs/01-开发规范/编码规范.md`
- 重要变更记录到 `docs/01-开发规范/变更日志.md`
