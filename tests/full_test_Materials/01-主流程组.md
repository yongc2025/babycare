# [主流程组] 核心业务主链路端到端状态机验证

## 用例 E2E-MAIN-001：机构创建 → 班级创建 → 员工创建 → 班级分配

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-001 |
| **前置数据条件** | `{{adminToken}}` 已登录 |
| **操作步骤** | **Step 1**: `POST /organization/create` — 创建机构，body: `{"name":"好芽儿测试园","contactPhone":"13800138000","address":"测试地址"}` → 响应取 `{{orgId}}`<br>**Step 2**: `POST /classroom/create` — 创建班级，body: `{"organizationId":"{{orgId}}","name":"豆芽一班","capacity":20}` → 响应取 `{{classroomId}}`<br>**Step 3**: `POST /staff/create` — 创建园长员工，body: `{"organizationId":"{{orgId}}","userId":2,"role":"DIRECTOR"}` → 响应取 `{{staffDirectorId}}`<br>**Step 4**: `POST /staff/create` — 创建教师，body: `{"organizationId":"{{orgId}}","userId":3,"role":"TEACHER"}` → 响应取 `{{staffTeacherId}}`<br>**Step 5**: `POST /staff/create` — 创建保育员，body: `{"organizationId":"{{orgId}}","userId":4,"role":"CAREGIVER"}` → 响应取 `{{staffCaregiverId}}` |
| **预期数据库变更** | `Organization` 表新增 1 条记录，`status='ACTIVE'`，`id={{orgId}}`<br>`Classroom` 表新增 1 条记录，`organization_id={{orgId}}`，`status='ACTIVE'`<br>`Staff` 表新增 3 条记录，`organization_id={{orgId}}` |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<orgId>}}`<br>Step 2: `{"success":true,"data":{"id":<classroomId>}}`<br>Step 3-5: `{"success":true,"data":{"id":<staffId>}}` |

---

## 用例 E2E-MAIN-002：入托全流程状态机（PENDING → HEALTH_CHECK → ACTIVE）

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-002 |
| **前置数据条件** | `{{adminToken}}`, `{{orgId}}`, `{{classroomId}}` 已就绪；`{{familyId}}`, `{{babyId}}` 已创建 |
| **操作步骤** | **Step 1**: `POST /enrollment/create` — 创建入托档案（PENDING），body: `{"babyId":"{{babyId}}","organizationId":"{{orgId}}","classroomId":"{{classroomId}}"}` → 响应取 `{{enrollmentId}}`<br>**Step 2**: `GET /enrollment/{{enrollmentId}}` — 验证初始状态 = PENDING<br>**Step 3**: `POST /enrollment/{{enrollmentId}}/review` — 园长审核通过 → HEALTH_CHECK，body: `{"approved":true}`<br>**Step 4**: `GET /enrollment/{{enrollmentId}}` — 验证状态 = HEALTH_CHECK，`reviewed_by` 非空，`reviewed_at` 已更新<br>**Step 5**: `POST /enrollment/{{enrollmentId}}/health-check` — 保健员健康审核通过 → ACTIVE，body: `{"approved":true}`<br>**Step 6**: `GET /enrollment/{{enrollmentId}}` — 验证最终状态 = ACTIVE，`enrolled_at` 已设置 |
| **预期数据库变更** | Step 1 后: `Enrollment.status='PENDING'`<br>Step 3 后: `Enrollment.status='HEALTH_CHECK'`，`Enrollment.reviewed_by` 非空，`Enrollment.updated_at` 更新<br>Step 5 后: `Enrollment.status='ACTIVE'`，`Enrollment.enrolled_at` 为当天日期<br>`EnrollmentStatusHistory` 表新增 3 条记录（PENDING→HEALTH_CHECK→ACTIVE） |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<enrollmentId>,"status":"PENDING"}}`<br>Step 2: `{"success":true,"data":{"status":"PENDING"}}`<br>Step 3: `{"success":true,"data":{"status":"HEALTH_CHECK"}}`<br>Step 4: `{"success":true,"data":{"status":"HEALTH_CHECK","reviewedBy":非空,"reviewedAt":非空}}`<br>Step 5: `{"success":true,"data":{"status":"ACTIVE"}}`<br>Step 6: `{"success":true,"data":{"status":"ACTIVE","enrolledAt":非空}}` |

---

## 用例 E2E-MAIN-003：考勤签到 → 照护记录 → 健康观察 → 日报生成 → 日报发布闭环

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-003 |
| **前置数据条件** | `{{enrollmentId}}` 状态为 ACTIVE；`{{teacherToken}}` 已登录；`{{classroomId}}` 可用 |
| **操作步骤** | **Step 1**: `POST /attendance/check-in` — 考勤签到，body: `{"enrollmentId":"{{enrollmentId}}","attendanceDate":"2026-07-19"}`<br>**Step 2**: `GET /attendance/classroom/{{classroomId}}?date=2026-07-19` — 验证考勤状态为 CHECKED_IN<br>**Step 3**: `POST /care-record/create` — 创建喂养记录，body: `{"enrollmentId":"{{enrollmentId}}","recordDate":"2026-07-19","recordTime":"2026-07-19T08:30:00","type":"FEEDING","amount":200,"unit":"ml"}` → 取 `{{careRecordId}}`<br>**Step 4**: `POST /care-record/create` — 创建睡眠记录，body: `{"enrollmentId":"{{enrollmentId}}","recordDate":"2026-07-19","recordTime":"2026-07-19T12:30:00","type":"SLEEP","startedAt":"2026-07-19T12:00:00","endedAt":"2026-07-19T14:30:00"}`<br>**Step 5**: `POST /health-observation/create` — 创建晨检记录，body: `{"enrollmentId":"{{enrollmentId}}","observationDate":"2026-07-19","type":"MORNING_CHECK","temperature":36.5,"abnormal":false,"followUpRequired":false}` → 取 `{{healthObsId}}`<br>**Step 6**: `POST /daily-report/generate` — 生成日报草稿，body: `{"enrollmentId":"{{enrollmentId}}","reportDate":"2026-07-19"}` → 取 `{{dailyReportId}}`<br>**Step 7**: `PUT /daily-report/{{dailyReportId}}` — 编辑日报，body: `{"teacherComment":"宝宝今天表现很棒！"}`<br>**Step 8**: `POST /daily-report/{{dailyReportId}}/publish` — 发布日报<br>**Step 9**: `GET /daily-report/{{dailyReportId}}` — 验证状态为 PUBLISHED |
| **预期数据库变更** | `AttendanceRecord` 新增 1 条，`status='CHECKED_IN'`，`check_in_at` 非空<br>`CareRecord` 新增 2 条，`type='FEEDING'` 和 `type='SLEEP'`，`enrollment_id={{enrollmentId}}`<br>`HealthObservation` 新增 1 条，`type='MORNING_CHECK'`，`abnormal=false`<br>`DailyReport` 新增 1 条，最终 `status='PUBLISHED'`，`summary` 含照护摘要 |
| **预期API响应** | Step 1-5: 均返回 `{"success":true,"data":{...}}`<br>Step 6: `{"success":true,"data":{"id":<reportId>,"status":"DRAFT"}}`<br>Step 8: `{"success":true,"data":{"status":"PUBLISHED"}}`<br>Step 9: `{"success":true,"data":{"status":"PUBLISHED","teacherComment":"宝宝今天表现很棒！"}}` |

---

## 用例 E2E-MAIN-004：请假申请 → 审批通过 → 考勤自动变为 LEAVE

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-004 |
| **前置数据条件** | `{{enrollmentId}}` 状态 ACTIVE；父母 `{{parentToken}}` 已登录；教师 `{{teacherToken}}` 已登录 |
| **操作步骤** | **Step 1**: `POST /attendance/leave/request` — 家长提交请假申请，body: `{"enrollmentId":"{{enrollmentId}}","startDate":"2026-07-21","endDate":"2026-07-22","type":"SICK","reason":"宝宝感冒"}` → 取 `{{leaveRequestId}}`<br>**Step 2**: `GET /attendance/leave/{{leaveRequestId}}` — 验证状态 = PENDING<br>**Step 3**: `POST /attendance/leave/{{leaveRequestId}}/approve` — 教师审批通过<br>**Step 4**: `GET /attendance/classroom/{{classroomId}}?date=2026-07-21` — 验证当天考勤状态 = LEAVE<br>**Step 5**: `GET /attendance/classroom/{{classroomId}}?date=2026-07-22` — 验证第二天考勤状态 = LEAVE |
| **预期数据库变更** | `LeaveRequest.status='APPROVED'`，`reviewed_by` 非空，`reviewed_at` 更新<br>2026-07-21 和 2026-07-22 各生成 1 条 `AttendanceRecord`，`status='LEAVE'`，`enrollment_id={{enrollmentId}}` |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<leaveId>,"status":"PENDING"}}`<br>Step 3: `{"success":true,"data":{"status":"APPROVED"}}`<br>Step 4-5: 返回数据中对应 enrollment 的 status = "LEAVE" |

---

## 用例 E2E-MAIN-005：用药委托 → 审批通过 → 用药执行 → 家长可见

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-005 |
| **前置数据条件** | `{{enrollmentId}}` ACTIVE；`{{parentToken}}` 登录；`{{healthToken}}` 登录 |
| **操作步骤** | **Step 1**: `POST /medication-care/request/create` — 家长提交用药委托，body: `{"enrollmentId":"{{enrollmentId}}","medicineName":"小儿感冒冲剂","dosage":"5ml","frequency":"每日3次","startDate":"2026-07-19","endDate":"2026-07-21"}` → 取 `{{medicationRequestId}}`<br>**Step 2**: `GET /medication-care/request/{{medicationRequestId}}` — 验证 PENDING<br>**Step 3**: `POST /medication-care/request/{{medicationRequestId}}/approve` — 保健员审批通过<br>**Step 4**: `POST /medication-care/administration/create` — 记录用药执行，body: `{"medicationRequestId":"{{medicationRequestId}}","administeredAt":"2026-07-19T08:30:00","actualDosage":"5ml","reactionObserved":false}`<br>**Step 5**: `GET /medication-care/request/baby/{{babyId}}` — 家长用 `{{parentToken}}` 查看，应能看到该记录 |
| **预期数据库变更** | `MedicationRequest.status='APPROVED'`，`reviewed_by` 非空<br>`MedicationAdministration` 新增 1 条，`medication_request_id={{medicationRequestId}}`，`actual_dosage='5ml'` |
| **预期API响应** | Step 3: `{"success":true,"data":{"status":"APPROVED"}}`<br>Step 4: `{"success":true,"data":{"id":<adminId>}}`<br>Step 5: 返回列表中包含该用药记录 |

---

## 用例 E2E-MAIN-006：事故上报 → 处理中 → 关闭 → 家长确认

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-006 |
| **前置数据条件** | `{{enrollmentId}}` ACTIVE；`{{teacherToken}}` 登录；`{{parentToken}}` 登录；`{{adminToken}}` 登录 |
| **操作步骤** | **Step 1**: `POST /incident-report/create` — 教师上报事故，body: `{"enrollmentId":"{{enrollmentId}}","type":"INJURY","severity":"MEDIUM","title":"摔倒擦伤","description":"户外活动时摔倒，膝盖擦伤","occurredAt":"2026-07-19T10:30:00","parentNotified":true}` → 取 `{{incidentReportId}}`<br>**Step 2**: `GET /incident-report/{{incidentReportId}}` — 验证 status = OPEN<br>**Step 3**: `PUT /incident-report/{{incidentReportId}}` — 更新处理过程，body: `{"handlingProcess":"已消毒包扎，安抚宝宝"}`<br>**Step 4**: `POST /incident-report/{{incidentReportId}}/close` — 园长关闭事故<br>**Step 5**: `POST /incident-report/{{incidentReportId}}/parent-confirm` — 家长确认 |
| **预期数据库变更** | `IncidentReport.status='CLOSED'`，`handling_process` 非空<br>`IncidentReport.parent_confirmed=true`，`parent_confirmed_at` 非空 |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<incidentId>,"status":"OPEN"}}`<br>Step 4: `{"success":true,"data":{"status":"CLOSED"}}`<br>Step 5: `{"success":true,"data":{"parentConfirmed":true}}` |

---

## 用例 E2E-MAIN-007：通知公告 → 发布 → 家长已读回执

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-007 |
| **前置数据条件** | `{{adminToken}}` 登录；`{{classroomId}}`；`{{parentToken}}` 登录 |
| **操作步骤** | **Step 1**: `POST /announcement/create` — 创建通知草稿，body: `{"organizationId":"{{orgId}}","classroomId":"{{classroomId}}","scope":"CLASSROOM","title":"明日家长会通知","content":"明天下午3点开家长会","requireReceipt":true}` → 取 `{{announcementId}}`<br>**Step 2**: `POST /announcement/{{announcementId}}/publish` — 发布通知<br>**Step 3**: `GET /announcement/classroom/{{classroomId}}` — 用 `{{parentToken}}` 查看，应可见该通知<br>**Step 4**: `POST /announcement/{{announcementId}}/read` — 家长标记已读 |
| **预期数据库变更** | `Announcement.status='PUBLISHED'`，`published_at` 非空<br>`AnnouncementReceipt` 新增 1 条，`announcement_id={{announcementId}}`，`user_id` 为家长用户 ID |
| **预期API响应** | Step 2: `{"success":true,"data":{"status":"PUBLISHED"}}`<br>Step 3: 返回列表中包含该通知<br>Step 4: `{"success":true}` |

---

## 用例 E2E-MAIN-008：招生线索 → 跟进 → 试托 → 转入托档案

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-008 |
| **前置数据条件** | `{{admToken}}` 招生登录；`{{adminToken}}` 登录；`{{orgId}}` |
| **操作步骤** | **Step 1**: `POST /admission-lead/create` — 创建招生线索，body: `{"organizationId":"{{orgId}}","babyName":"乐乐","babyGender":"MALE","birthDate":"2024-01-15","parentName":"乐乐妈","parentPhone":"13900139000","source":"ONLINE"}` → 取 `{{leadId}}`<br>**Step 2**: `GET /admission-lead/{{leadId}}` — 验证状态<br>**Step 3**: `POST /admission-lead/{{leadId}}/follow-up` — 添加跟进记录，body: `{"content":"已电话沟通，明天来园参观"}`<br>**Step 4**: `POST /admission-lead/{{leadId}}/trial/start` — 开始试托，body: `{"trialStartDate":"2026-07-21","trialEndDate":"2026-07-25"}`<br>**Step 5**: `POST /admission-lead/{{leadId}}/review` — 报名审核通过，body: `{"approved":true,"classroomId":"{{classroomId}}"}`<br>**Step 6**: `POST /admission-lead/{{leadId}}/convert` — 转为入托档案 → 返回 `{{newEnrollmentId}}` |
| **预期数据库变更** | `AdmissionLead` 最终状态为 `ENROLLED`<br>`FollowUpRecord` 新增 1 条<br>试托后 `AdmissionLead.trial_start_date` 和 `trial_end_date` 非空<br>`Enrollment` 新增 1 条，`status='PENDING'` |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<leadId>}}`<br>Step 5: `{"success":true,"data":{"status":"APPROVED"}}`<br>Step 6: `{"success":true,"data":{"enrollmentId":<newEnrollmentId>}}` |

---

## 用例 E2E-MAIN-009：收费项目 → 生成账单 → 标记支付

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-009 |
| **前置数据条件** | `{{adminToken}}` 登录；`{{orgId}}`；`{{enrollmentId}}` |
| **操作步骤** | **Step 1**: `POST /billing/fee-item/create` — 创建收费项目，body: `{"organizationId":"{{orgId}}","name":"托育费","amount":3000.00}` → 取 `{{feeItemId}}`<br>**Step 2**: `GET /billing/fee-item/organization/{{orgId}}` — 验证列表包含该项目<br>**Step 3**: `POST /billing/bill/create` — 生成账单，body: `{"organizationId":"{{orgId}}","enrollmentId":"{{enrollmentId}}","feeItemId":"{{feeItemId}}","title":"7月托育费","amount":3000.00,"dueDate":"2026-07-31"}` → 取 `{{billId}}`<br>**Step 4**: `GET /billing/bill/{{billId}}` — 验证 status = UNPAID<br>**Step 5**: `POST /billing/bill/{{billId}}/paid` — 标记已支付，body: `{"paymentMethod":"CASH"}`<br>**Step 6**: `GET /billing/bill/{{billId}}` — 验证 status = PAID，`paid_at` 非空 |
| **预期数据库变更** | `FeeItem` 新增 1 条，`status='ACTIVE'`<br>`BillingStatement` 新增 1 条，初始 `status='UNPAID'`，最终 `status='PAID'`，`paid_at` 非空 |
| **预期API响应** | Step 3: `{"success":true,"data":{"id":<billId>,"status":"UNPAID"}}`<br>Step 5: `{"success":true,"data":{"status":"PAID"}}` |

---

## 用例 E2E-MAIN-010：日报审核流程（DRAFT → PENDING_APPROVAL → PUBLISHED）

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-010 |
| **前置数据条件** | `{{enrollmentId}}` ACTIVE；`{{teacherToken}}` 登录；`{{adminToken}}`（园长）登录；机构已开启 `dailyReportApprovalRequired=true` |
| **操作步骤** | **Step 1**: `POST /daily-report/generate` — 教师生成日报草稿，body: `{"enrollmentId":"{{enrollmentId}}","reportDate":"2026-07-19"}` → 取 `{{dailyReportId}}`<br>**Step 2**: `POST /daily-report/{{dailyReportId}}/submit` — 教师提交审核，状态 → PENDING_APPROVAL<br>**Step 3**: `GET /daily-report/{{dailyReportId}}` — 验证 status = PENDING_APPROVAL<br>**Step 4**: `POST /daily-report/{{dailyReportId}}/approve` — 园长审核通过 → PUBLISHED<br>**Step 5**: `GET /daily-report/{{dailyReportId}}` — 验证 status = PUBLISHED，`reviewed_by` 为园长，`reviewed_at` 非空<br>**Step 6**: 用 `{{parentToken}}` 调用 `GET /daily-report/baby/{{babyId}}?date=2026-07-19` — 家长可见该日报 |
| **预期数据库变更** | `DailyReport` 状态流转：`DRAFT → PENDING_APPROVAL → PUBLISHED`<br>`published_at` 和 `reviewed_at` 均非空 |
| **预期API响应** | Step 2: `{"success":true,"data":{"status":"PENDING_APPROVAL"}}`<br>Step 4: `{"success":true,"data":{"status":"PUBLISHED"}}`<br>Step 6: 返回的日报数据中 status = "PUBLISHED" |

---

## 用例 E2E-MAIN-011：安全台账周期任务 → 生成 → 处理 → 关闭

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-011 |
| **前置数据条件** | `{{adminToken}}`（园长/安全员）登录；`{{orgId}}` |
| **操作步骤** | **Step 1**: `POST /safety-ledger/template/create` — 创建台账模板，body: `{"organizationId":"{{orgId}}","name":"每日消毒记录","type":"DISINFECTION","frequency":"DAILY"}` → 取模板 ID<br>**Step 2**: `POST /safety-ledger/generate-tasks/{{orgId}}` — 根据模板生成到期台账<br>**Step 3**: `GET /safety-ledger/organization/{{orgId}}` — 查看台账列表 → 取 `{{safetyLedgerId}}`<br>**Step 4**: `PUT /safety-ledger/{{safetyLedgerId}}` — 填写台账内容，body: `{"description":"已完成教室消毒"}`<br>**Step 5**: `POST /safety-ledger/{{safetyLedgerId}}/close` — 关闭台账 |
| **预期数据库变更** | `SafetyLedgerTemplate` 新增 1 条<br>`SafetyLedger` 新增至少 1 条，`template_id` 关联，最终 `status='CLOSED'` |
| **预期API响应** | Step 1: `{"success":true,"data":{"id":<templateId>}}`<br>Step 2: `{"success":true,"data":{"count":>=1}}`<br>Step 5: `{"success":true,"data":{"status":"CLOSED"}}` |

---

## 用例 E2E-MAIN-012：食谱发布 → 进食记录 → 营养分析

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-012 |
| **前置数据条件** | `{{adminToken}}` 登录；`{{orgId}}`；`{{enrollmentId}}` |
| **操作步骤** | **Step 1**: `POST /meal-plan/create` — 创建食谱，body: `{"organizationId":"{{orgId}}","date":"2026-07-19","mealType":"LUNCH","menu":"胡萝卜炒肉、清炒小白菜、米饭","nutritionInfo":"蛋白质15g、碳水30g"}` → 取 `{{mealPlanId}}`<br>**Step 2**: `POST /meal-plan/{{mealPlanId}}/publish` — 发布食谱<br>**Step 3**: `POST /meal-plan/intake/record` — 记录宝宝进食，body: `{"mealPlanId":"{{mealPlanId}}","enrollmentId":"{{enrollmentId}}","intakePercentage":80,"allergyReaction":false}`<br>**Step 4**: `GET /meal-plan/analysis/organization/{{orgId}}` — 查看营养分析报表 |
| **预期数据库变更** | `MealPlan` 新增 1 条，`status='PUBLISHED'`<br>`MealIntakeRecord` 新增 1 条，`enrollment_id={{enrollmentId}}` |
| **预期API响应** | Step 2: `{"success":true,"data":{"status":"PUBLISHED"}}`<br>Step 4: 返回营养分析聚合数据 |

---

## 用例 E2E-MAIN-013：园长工作台聚合数据一致性验证

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-013 |
| **前置数据条件** | 前置用例 E2E-MAIN-001~012 全部执行完毕，机构有 ACTIVE 入托档案、日报、考勤、缴费记录 |
| **操作步骤** | **Step 1**: `GET /director-dashboard/organization/{{orgId}}?date=2026-07-19` — 查看机构运营概览<br>**Step 2**: `GET /director-dashboard/workbench/{{orgId}}` — 查看园长工作台（待办+风险） |
| **预期数据库变更** | 无变更（只读查询） |
| **预期API响应** | Step 1: 返回数据中 `activeEnrollmentCount` ≥ 1，`checkedInCount` 与实际签到一致，`attendanceRate` 为合理百分比，`unpaidBillCount` 与实际未支付账单数一致<br>Step 2: 返回数据含待办事项和风险预警，数据与业务记录一致 |

---

## 用例 E2E-MAIN-014：授权接送人 → 委托接送 → 审核 → 接送码生成 → 离园签退

| 项目 | 内容 |
|---|---|
| **用例ID** | E2E-MAIN-014 |
| **前置数据条件** | `{{enrollmentId}}` ACTIVE；`{{parentToken}}` 登录；`{{adminToken}}`（园长/教师）登录 |
| **操作步骤** | **Step 1**: `POST /pickup/person/create` — 家长创建授权接送人，body: `{"enrollmentId":"{{enrollmentId}}","name":"爷爷","relationship":"祖父","phone":"13700137000"}` → 取 `{{pickupPersonId}}`<br>**Step 2**: `GET /pickup/person/baby/{{babyId}}` — 验证列表包含该授权人<br>**Step 3**: `POST /pickup/delegation/create` — 创建委托接送，body: `{"enrollmentId":"{{enrollmentId}}","pickupDate":"2026-07-19","pickupPersonName":"临时阿姨","pickupPhone":"13600136000","reason":"加班"}` → 取 `{{delegationId}}`<br>**Step 4**: `POST /pickup/delegation/{{delegationId}}/approve` — 教师/园长审核通过<br>**Step 5**: `GET /pickup/delegation/{{delegationId}}` — 验证 status = APPROVED，`pickupCode` 非空<br>**Step 6**: `POST /attendance/check-out` — 离园签退，body: `{"enrollmentId":"{{enrollmentId}}","attendanceDate":"2026-07-19","pickupPersonName":"临时阿姨"}` |
| **预期数据库变更** | `AuthorizedPickupPerson` 新增 1 条，`status='ACTIVE'`<br>`PickupDelegation` 最终 `status='APPROVED'`，`pickup_code` 非空<br>`AttendanceRecord` 当天记录 `status` 从 CHECKED_IN 变更为 CHECKED_OUT，`check_out_at` 非空 |
| **预期API响应** | Step 4: `{"success":true,"data":{"status":"APPROVED","pickupCode":"<6位码>"}}`<br>Step 6: `{"success":true,"data":{"status":"CHECKED_OUT"}}` |
